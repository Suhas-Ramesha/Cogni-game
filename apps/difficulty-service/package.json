{
  "name": "@cognigame/difficulty-service",
  "version": "0.9.0",
  "private": true,
  "scripts": {
    "postinstall": "node -e \"if(process.env.EAS_BUILD)process.exit(0); require('child_process').execSync('python3 -m venv .venv && .venv/bin/pip install -r requirements.txt',{stdio:'inherit'})\"",
    "start": ".venv/bin/uvicorn app.main:app --reload --port 8001 --host 0.0.0.0",
    "test": ".venv/bin/pytest tests -q",
    "install:py": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt"
  }
}
